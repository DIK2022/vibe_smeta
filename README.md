# PDF-планировка → 3D-модель → Excel-смета

Прототип системы, которая превращает планировку квартиры из PDF
в упрощённую 3D-модель, расставляет мебель, считает объёмы работ
и выгружает результат в Excel — со **сверкой источников данных**.

Проект собран как ответ на тестовое задание на вакансию
«Вайбкодер / AI-разработчик / Python — автоматизация строительных смет».

---

## Содержание

- [PDF-планировка → 3D-модель → Excel-смета](#pdf-планировка--3d-модель--excel-смета)
  - [Содержание](#содержание)
  - [Что делает](#что-делает)
  - [Стек](#стек)
  - [Быстрый старт](#быстрый-старт)
    - [Установка `uv`](#установка-uv)
    - [Запуск](#запуск)
  - [Структура проекта](#структура-проекта)
    - [Назначение файлов](#назначение-файлов)
  - [Архитектура](#архитектура)
    - [Поток данных](#поток-данных)
    - [Ключевые решения](#ключевые-решения)
  - [Обнаруженные проблемы в исходных данных](#обнаруженные-проблемы-в-исходных-данных)
    - [1. Расхождения площадей](#1-расхождения-площадей)
    - [2. Конфликт назначения помещения](#2-конфликт-назначения-помещения)
    - [3. Возможные причины расхождений](#3-возможные-причины-расхождений)
    - [4. Открытые вопросы к заказчику](#4-открытые-вопросы-к-заказчику)
  - [Как система работает с противоречиями](#как-система-работает-с-противоречиями)
  - [Что автоматизировано, что вручную](#что-автоматизировано-что-вручную)
    - [Автоматизировано](#автоматизировано)
    - [Вручную](#вручную)
  - [Ограничения прототипа](#ограничения-прототипа)
  - [Результаты](#результаты)
    - [Консольный отчёт](#консольный-отчёт)
    - [Excel `volumes.xlsx`](#excel-volumesxlsx)
    - [3D-модель `apartment.glb`](#3d-модель-apartmentglb)
  - [Развитие](#развитие)
    - [Ближайшие шаги](#ближайшие-шаги)
    - [Средний срок](#средний-срок)
    - [Долгосрочно](#долгосрочно)
  - [Лицензия](#лицензия)
  - [Контакты](#контакты)

---

## Что делает

Конвейер обработки:

```
PDF-планировка → 2D-данные → 3D-модель → расстановка мебели
             → расчёт объёмов → сверка источников → Excel-смета
```

На выходе:

- `apartment.glb` — 3D-модель квартиры (открывается в Blender, Windows 3D Viewer, онлайн-вьюерах).
- `volumes.xlsx` — Excel с тремя листами:
  - **Объёмы работ** — площади пола, стен, потолков, периметры, количество дверей и окон.
  - **Сверка источников** — сравнение площадей из экспликации и кружков на чертеже с цветовой подсветкой.
  - **Открытые вопросы** — позиции, по которым система не может принять решение автоматически.

Плюс интерактивное Streamlit-демо для наглядного просмотра.

---

## Стек

| Компонент | Версия | Зачем |
|---|---|---|
| Python | 3.13 | Основной язык |
| uv | latest | Управление зависимостями и окружением |
| NumPy | ≥2.1 | Геометрия, меши |
| Pandas | ≥2.2 | Таблицы, Excel |
| trimesh | ≥4.5 | 3D-меши, экспорт в `.glb` |
| openpyxl | ≥3.1 | Запись Excel с форматированием |
| Streamlit | ≥1.49 | Интерактивное демо (бонус) |
| Plotly | ≥5.24 | 3D-визуализация (бонус) |

Опционально для развития:

- **Pillow / pdf2image** — рендер PDF в PNG.
- **OpenCV** — детекция стен и проёмов на растре.
- **pdfplumber** — извлечение текста и размеров из векторного PDF.
- **Anthropic / OpenAI SDK** — LLM Vision для авто-извлечения координат.
- **ifcopenshell** — экспорт в IFC для BIM-совместимости.

---

## Быстрый старт

### Установка `uv`

Если ещё не установлен:

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### Запуск

```bash
# 1. Установить Python 3.13 и зависимости
uv python install 3.13
uv sync

# 2. Сгенерировать обязательные артефакты (по ТЗ)
uv run build_model.py

# 3. Опционально: интерактивное демо
uv run streamlit run app.py
```

После шага 2 в корне появятся:

```
apartment.glb
volumes.xlsx
```

После шага 3 откроется браузер на `http://localhost:8501`.

---

## Структура проекта

```
vibe_smeta/
├── core.py               # Общая логика: данные, геометрия, сверка, экспорт
├── build_model.py        # CLI: создаёт apartment.glb и volumes.xlsx
├── app.py                # Streamlit-демо (бонус)
├── pyproject.toml        # Зависимости и метаданные проекта
├── uv.lock               # Зафиксированные версии зависимостей
├── .python-version       # Версия Python для uv
├── .gitignore
└── README.md             # Этот файл
```

### Назначение файлов

**`core.py`** — вся логика в одном месте:

- `EXPLICATION` — площади из таблицы на чертеже.
- `DRAWING_MARKS` — площади с кружков на плане.
- `NAMING_CONFLICTS` — конфликты назначения помещений.
- `ROOMS` — геометрия комнат (полигоны).
- `FURNITURE` — типовые размеры мебели.
- `compare_sources()` — сверка экспликации и чертежа.
- `get_open_questions()` — список вопросов к заказчику.
- `build_all()` — сборка 3D-меша.
- `compute_volumes()` — расчёт объёмов.
- `export_obj_file()`, `export_excel_file()` — экспорт.

**`build_model.py`** — тонкая CLI-обёртка: вызывает `core.py`, создаёт файлы, печатает отчёт в консоль.

**`app.py`** — Streamlit-интерфейс: вкладки «3D-модель», «Сверка источников», «Открытые вопросы», «Объёмы», «Excel», «О решении».

---

## Архитектура

### Поток данных

```
┌──────────────────────┐
│  PDF-планировка      │
│  (вход, вручную)     │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  EXPLICATION         │  ← площади из таблицы
│  DRAWING_MARKS       │  ← площади с кружков
│  ROOMS (полигоны)    │  ← координаты для 3D
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    ▼             ▼
┌────────┐   ┌──────────────┐
│ 3D-меш │   │  Сверка      │
│ (.glb) │   │  источников  │
└────────┘   └──────┬───────┘
                    │
                    ▼
            ┌──────────────┐
            │ Объёмы работ │
            │  + вопросы   │
            └──────┬───────┘
                   │
                   ▼
            ┌──────────────┐
            │ volumes.xlsx │
            │  (3 листа)   │
            └──────────────┘
```

### Ключевые решения

**1. Комнаты — соседние полигоны, не вложенные.**

Комнаты стыкуются вплотную, но не перекрываются. Это избегает
удвоения площадей, z-fighting полов и дублирования стен.

**2. Дедупликация стен.**

Общая стена между двумя комнатами рисуется один раз. Реализовано
через множество нормализованных рёбер: `(min_x, min_y, max_x, max_y)`.

**3. Полигоны вместо прямоугольников.**

Комнаты описываются списком вершин, а не `(x0, y0, x1, y1)`.
Это позволяет задавать нестандартные формы, если они есть на плане.

**4. Два источника площадей.**

Экспликация — официальный документ, по которому работает смета.
Кружки на чертеже — визуальные метки. Система хранит оба источника
и сравнивает их.

**5. Три листа Excel.**

Объёмы, сверка, открытые вопросы — разделены, чтобы сметчик видел
и результат, и проблемы данных, и точки, где нужно его решение.

---

## Обнаруженные проблемы в исходных данных

При разборе PDF выявлены **противоречия между экспликацией**
(таблица справа на чертеже) **и кружками** на плане.

### 1. Расхождения площадей

| Помещение | Экспликация | Чертёж | Разница |
|---|---|---|---|
| Прихожая | 9,87 м² | 3,40 м² | −65 % |
| Кухня-Гостиная | 35,45 м² | 52,07 м² | +47 % |
| Спальня | 12,42 м² | 9,74 м² | −22 % |
| Спальня №2 | 17,50 м² | 20,72 м² | +18 % |
| Санузел | 2,09 м² | 2,09 м² | 0 % ✅ |
| Ванная | 6,73 м² | 6,80 м² | +1 % ✅ |
| Балкон | 9,50 м² | — | нет кружка |
| **ИТОГО** | **93,56 м²** | **94,82 м²** | **+1,3 %** |

**Совпадают только санузел и ванная.** Остальное расходится
в разы — особенно Прихожая (в 3 раза) и Кухня-Гостиная (в 1,5 раза).

### 2. Конфликт назначения помещения

Кружок **⑥** на плане стоит на помещении, которое **по форме
и толщине стен является балконом** — это выступающая часть
с остеклением по периметру и тонкими стенами.

Но в экспликации это помещение записано как **«Спальня»** (12,42 м²).

При этом строка **«Балкон»** (9,50 м²) в экспликации **не имеет
собственного кружка** на плане.

### 3. Возможные причины расхождений

- **Разные определения площади.** БТИ считает «общую площадь»
  без балконов и с понижающим коэффициентом для лоджий (0,5).
  Дизайнер — по внутренним стенам.
- **Разные версии проекта.** Чертёж может быть до перепланировки,
  а экспликация — после (или наоборот).
- **ГКЛ-конструкции.** Гипсокартонные короба, ниши, встроенные
  шкафы по-разному учитываются в площади.
- **Разные источники.** Кружки часто ставятся для визуальной
  ориентировки, экспликация — официальный документ БТИ.

### 4. Открытые вопросы к заказчику

1. Какую цифру использовать по расходящимся позициям — из экспликации
   или с чертежа?
2. Есть ли между документами перепланировка?
3. Что считать балконом — только кружок ⑥, или есть отдельное помещение?
4. Как применяется понижающий коэффициент для балкона в смете?

Все эти вопросы автоматически попадают на лист **«Открытые вопросы»**
в Excel и на вкладку в Streamlit-демо.

---

## Как система работает с противоречиями

Вместо того чтобы молча выбрать одну цифру, реализована **сверка источников**:

**Лист «Сверка источников»** в Excel — с цветовой индикацией:

- 🟩 **зелёный** — OK (расхождение ≤ 5 %).
- 🟥 **красный** — существенное расхождение.
- 🟨 **жёлтый** — конфликт назначения помещения.
- ⬜ **серый** — итоговая строка.

**Лист «Открытые вопросы»** — список позиций, по которым нужно
решение заказчика.

**В Streamlit** — отдельные вкладки «Сверка источников»
и «Открытые вопросы». В сайдбаре — переключатель источника:
экспликация / чертёж / геометрия.

По умолчанию используется **экспликация** — официальный документ,
по которому работает смета. Чертёж — для визуализации геометрии.

---

## Что автоматизировано, что вручную

### Автоматизировано

- Построение 3D-модели: пол, стены, потолок (опционально), мебель.
- Расстановка мебели по правилам для каждого типа помещения
  (кровать к стене, кухня вдоль дальней стены, стол в центре и т. д.).
- Дедупликация стен: общая стена между комнатами рисуется один раз.
- Расчёт объёмов: площадь пола, стен (за вычетом проёмов), потолка,
  периметр, количество дверей и окон.
- **Сверка источников**: сравнение экспликации и чертежа с подсветкой.
- **Выявление конфликтов назначения** помещений.
- **Автоматическое формирование списка открытых вопросов.**
- Выгрузка в Excel с тремя листами.
- Экспорт 3D-модели в `.glb`.

### Вручную

- **Первичная оцифровка координат помещений с PDF.** Прочитал
  размерные линии с чертежа, задал полигоны в `ROOMS`. Точность ±10–15 %.
- Проверка логичности расстановки мебели.
- Выбор источника площадей для сметы (по умолчанию — экспликация).

---

## Ограничения прототипа

Прототип **не является точной копией** планировки:

- **Геометрия приблизительная.** Координаты оцифрованы вручную
  с точностью ±10–15 %. Пропорции комнат близки к оригиналу,
  но не совпадают 1:1.
- **Толщина стен единая** — 80 мм. На плане есть несущие до 400 мм.
- **Проёмы (двери, окна) не вырезаны в 3D** — только учтены
  в расчёте площади стен.
- **Форма балкона упрощена** до прямоугольника (на плане — 5009 × 1900 мм).
- **Мебель типовая**, расставлена по правилам, а не по дизайн-проекту.
- **Помещения описываются как соседние полигоны**, а не как пустота
  между стенами. Полноценная BIM-модель (Revit / IFC) — следующий этап.

Для **точной** геометрии требуется один из подходов:

| Подход | Точность | Трудоёмкость |
|---|---|---|
| CV-парсинг векторного PDF (OpenCV + HoughLines) | Высокая | 4–8 часов |
| LLM Vision API (Claude / GPT-4o) → JSON с полигонами | Средняя | 1–2 часа |
| Ручная оцифровка в QGIS / AutoCAD → GeoJSON | Высокая | 2–4 часа |
| Revit / ArchiCAD → экспорт IFC | Очень высокая | 8+ часов |

Это отражено в разделе «Развитие».

---

## Результаты

После запуска `uv run build_model.py`:

### Консольный отчёт

```
==============================================================================
СВЕРКА ИСХОДНЫХ ДАННЫХ: ЭКСПЛИКАЦИЯ vs ЧЕРТЁЖ
==============================================================================
      Помещение  Экспликация (м²)  Чертёж (м²)  Разница (м²)  Разница (%)                       Статус
       Прихожая              9.87         3.40         -6.47        -65.5                РАСХОЖДЕНИЕ
        Санузел              2.09         2.09          0.00          0.0                         OK
         Ванная              6.73         6.80          0.07          1.0                         OK
 Кухня-Гостиная             35.45        52.07         16.62         46.9                РАСХОЖДЕНИЕ
        Спальня             12.42         9.74         -2.68        -21.6  РАСХОЖДЕНИЕ ⚠ конфликт назначения
     Спальня №2             17.50        20.72          3.22         18.4                РАСХОЖДЕНИЕ
         Балкон              9.50         None          None         None            нет на чертеже
          ИТОГО             93.56        94.82          1.26          1.3                           —

⚠  Найдено расхождений: 4

==============================================================================
ОТКРЫТЫЕ ВОПРОСЫ К ЗАКАЗЧИКУ
==============================================================================

1. [Расхождение площадей] Прихожая
   Экспликация: 9.87 м², чертёж: 3.4 м² (разница -65.5 %).
   Какую цифру брать в смету?
...

✓ 3D-модель сохранена: apartment.glb
✓ Объёмы сохранены: volumes.xlsx
```

### Excel `volumes.xlsx`

**Лист 1 — Объёмы работ**

| Помещение | Показатель | Ед. изм. | Количество |
|---|---|---|---|
| Кухня-Гостиная | Площадь пола | м² | 35.45 |
| Кухня-Гостиная | Площадь стен | м² | 45.72 |
| Кухня-Гостиная | Площадь потолка | м² | 35.45 |
| ... | ... | ... | ... |
| **ИТОГО** | **Площадь пола** | **м²** | **93.56** |

**Лист 2 — Сверка источников**  
С цветовой подсветкой строк (зелёный / красный / жёлтый / серый).

**Лист 3 — Открытые вопросы**  
Таблица с колонками: Тип, Помещение, Вопрос, Возможные причины, Действие.

### 3D-модель `apartment.glb`

Открывается в:

- Blender (File → Import → glTF 2.0)
- Windows 3D Viewer
- macOS Preview
- Онлайн: https://gltf-viewer.donmccurdy.com

---

## Развитие

### Ближайшие шаги

1. **LLM Vision API (Claude / GPT-4o)** — авто-извлечение координат
   стен и помещений из PDF. Промпт: «Верни JSON с полигонами
   помещений, высотами, количеством проёмов».
2. **CV-парсинг векторного PDF** — OpenCV + HoughLines для детекции
   стен и проёмов на растре.
3. **Автоматическая сверка** экспликации, размерных линий и площадей
   с подсветкой несоответствий.
4. **Вырезание дверных и оконных проёмов** в 3D через
   `trimesh.boolean.difference`.

### Средний срок

5. **Интеграция с расценками заказчика** — merge объёмов с прайс-листом
   → автоматическая смета.
6. **BIM-интеграция** — экспорт в IFC через `ifcopenshell`
   для работы в Revit / ArchiCAD.
7. **Web-версия** — загрузка PDF через браузер, обработка на сервере,
   скачивание готовой сметы.
8. **База типовых решений** — библиотека расстановки мебели,
   подбор материалов по помещению.

### Долгосрочно

9. **Обработка проектной документации в целом** — не только планировок,
   но и развёрток стен, ведомостей, спецификаций.
10. **Автоматическое формирование смет** по нормативной базе
    (ФЕР, ТЕР) с учётом региональных коэффициентов.

---

## Лицензия

Тестовое задание. Не для коммерческого использования.

## Контакты

Денис Климук  
- Email: denisklimuk2@gmail.com  
- Телефон: +7 (961) 302-08-16  
- GitHub: https://github.com/DIK2022